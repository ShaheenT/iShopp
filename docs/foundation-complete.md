# Core foundation complete

The iShopp production repository now has the first coherent backend foundation: Next.js 15, Supabase boundaries, authentication session handling, core PostgreSQL entities, RLS, shopping lists, verification states and documented delivery controls.

The next implementation target is the verified specials API and ingestion pipeline.
